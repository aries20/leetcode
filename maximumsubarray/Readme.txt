We can do brute force but we can use Kadanes Algorithm
So here we need to find subarray such that it can led to maximum sum

in kadanes algorithm we have currsum and maxsum we
iterate through array with max sum and curr sum we change current sum 
everytime and max sum when we get a bigger currentsum

